// components/layout/PageHeader/PageHeader.tsx
// Layer 4 — PRESENTATIONAL: Page-level title + optional action slot

interface Props {
  title: string;
  description?: string;
  action?: React.ReactNode;
}

export function PageHeader(_props: Props) {
  return null;
}
