// features/payments/components/RecordPaymentModal/RecordPaymentModal.tsx
// Layer 4 — PRESENTATIONAL: Modal wrapper for recording a payment

interface Props {
  memberId: string;
  open: boolean;
  onClose: () => void;
}

export function RecordPaymentModal(_props: Props) {
  return null;
}
