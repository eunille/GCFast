// features/payments/components/PaymentStatusBadge/PaymentStatusBadge.tsx
// Layer 4 — PRESENTATIONAL: Renders paid/outstanding badge using theme tokens

import type { PaymentStatus } from "../../types/payment.types";

interface Props {
  status: PaymentStatus;
}

export function PaymentStatusBadge(_props: Props) {
  return null;
}
