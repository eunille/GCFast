// app/(treasurer)/members/[id]/page.tsx
// Layer 4 — PRESENTATIONAL: Individual member detail page

interface Props {
  params: Promise<{ id: string }>;
}

export default function MemberDetailPage(_props: Props) {
  return null;
}

